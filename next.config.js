/** @type {import('next').NextConfig} */
const nextConfig = {
    
    images: {
        domains: [
            // "res.cloudinary.com",
            "www.gmercyu.edu"
        ]
    }
}

module.exports = nextConfig
